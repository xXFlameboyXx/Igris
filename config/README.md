# Configuration

Runtime configuration is environment-driven and loaded by `igris.core.config`.
Use `.env.example` as the documented template.

Real secrets must come from local environment variables, a secret manager, or CI
secret storage. They must not be committed to this directory.

